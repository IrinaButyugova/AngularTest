import { Component } from '@angular/core';
     
@Component({
    selector: 'my-app',
    template: `<div>
    <get-change-model></get-change-model>
    </div>`
})
export class AppComponent { 
    name= '';
}